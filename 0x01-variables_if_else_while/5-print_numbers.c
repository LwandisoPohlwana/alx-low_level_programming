#include <stdio.h>

/**
 * main - prints a number untill the base 10 is reached.
 *
 * Return: must be 0.
*/

int main(void)
{
	int number;

	fot (number = 0; number <= 10; number++)
		printf("%d", number);

	printf('\n');

	return (0);
}

